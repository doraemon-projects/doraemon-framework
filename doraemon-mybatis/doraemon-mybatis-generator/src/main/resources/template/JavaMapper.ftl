package ${model.packageName}.mapper;

import ${model.packageName}.vo.${model.name};
import org.jfteam.mybatis.dao.BaseMapper;

/**
* Created with IntelliJ IDEA.
* Description: ${model.comment}访问数据库Mapper
* Author:      fengwenping
* Date:        ${.now}
*/
public interface ${model.name}Mapper<${model.name}, String> extends BaseMapper<${model.name}, String> {
}
